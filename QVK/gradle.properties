org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Public OAuth client id for VK's "Standalone application" login flow — not a secret
# (it ships inside every installed APK regardless), safe to default here so a plain
# `./gradlew assembleDebug` works out of the box. Override with -PVK_CLIENT_ID=... for a
# different registered VK app.
VK_CLIENT_ID=51395681
